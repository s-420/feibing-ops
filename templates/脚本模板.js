/**
 * 脚本元数据头（必填，agent 与人都靠它理解用途）
 *
 * @对象    <对象名，如：群>
 * @动作    <动作名，如：批量写>
 * @风险    <低 | 中 | 高>
 * @输入    <输入文件 / 参数说明>
 * @输出    <输出产物说明>
 * @验收    <如何判断执行成功>
 *
 * 一句话用途：<这个脚本做什么>
 *
 * 写操作必须：dryRun 预演 → 备份 → 小范围试跑 → 全量执行 → 复核
 * 凭据一律走环境变量，不硬编码、不提交
 */

async function main() {
  // TODO: 实现逻辑
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
