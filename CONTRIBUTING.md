# 上传规范（CONTRIBUTING）

> 本文件定义「业务操作封装与上传」的 8 项约定。任何成员、任何 agent 往本库新增 / 修改内容，都必须遵守。
> 目的只有一个：**保证结构规范、可复用、可交接**，而不是约束谁怎么上传。

## 8 项约定

### 1. 目录骨架固定

仓库目录固定为 6 部分，不允许随意新增顶层目录：

- `taxonomy/` 分类清单（唯一事实源）
- `skills/` skill 库
- `scripts/` 脚本库
- `records/` 业务留痕
- `templates/` 模板
- `CONTRIBUTING.md` 本规范

新需求进来，只在这 6 处之内动，不新增顶层结构。

### 2. 分类方法：对象 × 动作

- **对象** = 业务对象 / 功能域（如桌台、群、门店），做**一级目录**，回答「这一步归属什么」。
- **动作** = 操作行为（导出 / 批量写 / 匹配 / 排查），做**元数据字段**，回答「怎么安全执行」。
- **客户 / 数据** = 输入参数，**永不进目录**。

### 3. 命名约定

- 目录名 = 对象名（名词，如 `群`）。
- 文件名 = `对象-动作-说明`（如 `群-批量替换-群链接.js`），不拼音、不缩写、可读优先。

### 4. 元数据约定

每个脚本 / skill 头部必须带统一元数据头（见 `templates/`），至少含：
`对象 / 动作 / 风险等级 / 输入 / 输出 / 验收标准`。

### 5. 封装层级（三条路都进库）

| 情况 | 封装成 | 判断标准 |
|---|---|---|
| 输入输出确定 | 脚本 script | 只做一件事，无需判断 |
| 需要判断 / 多步组合 | skill | 要理解需求、决策、调用脚本 |
| 只能界面手点 | SOP 文档型 skill | 无 API / 脚本可调，记录操作步骤 + 检查点 |

### 6. 风险与验收

- **读操作**（导出 / 查询）：低风险，可直接跑。
- **写操作**（批量改 / 删）：高风险，必须 **dryRun 预演 → 备份 → 小范围试跑 → 全量执行 → 复核**，并保留回滚入口。

### 7. 留痕约定

每次真实业务处理，必须在 `records/` 留一条（格式见 `templates/留痕记录.md`），字段固定：
`时间 / 客户 / 对象 / 动作 / 处理人 / 结果 / 待办遗漏`。

### 8. 提交约定

- commit message 格式：`[对象] 动作：说明`（如 `[群] 新增：群链接批量替换脚本`）。
- 不直接 push `main`，走 feature 分支 + PR。
- 写操作脚本合入前，必须附 dryRun 结果。

## 变更流程

1. 新需求进来 → 先查 `taxonomy/index.md` 是否已有对应类型。
2. 有 → 复用已有脚本 / skill，处理后留痕，不改结构。
3. 无 → 按第 5 条选择封装层级 → 新增条目到 taxonomy → 按约定新增文件 → PR。
